[user]
	name = Greg Gannicott
	email = greg@gannicott.co.uk
[push]
	default = simple
[credential]
	helper = cache --timeout=7200
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
